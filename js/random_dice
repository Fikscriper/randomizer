const generation = document.getElementById('genDice');
const result = document.getElementById('resultDice');


function generateDice()
{
   const Dice = Math.floor(Math.random() * (6 - 1 + 1)) + 1;
    result.textContent = Dice;

    console.log(Dice);
}

generation.addEventListener('click', generateDice);